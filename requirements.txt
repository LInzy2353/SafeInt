torch==2.1.0+cu118
# SafeInt: Shielding Large Language Models from Jailbreak Attacks via Safety-Aware Representation Intervention

# 核心依赖
torch>=2.0.0
transformers>=4.28.1
datasets>=2.14.6
scikit-learn>=1.3.2
numpy>=1.24.0
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0
tqdm>=4.65.0
pillow>=9.5.0

# 模型相关
huggingface_hub>=0.16.0
accelerate>=0.20.0
sentencepiece
bitsandbytes>=0.40.0  # 用于量化模型

# 评估工具
# 注意：实际使用时，可能需要安装OpenAI或其他API包来调用GPT-4o-mini
# openai>=1.0.0  # 未包含在内，因为可能需要用户单独配置API密钥

# 其他工具
scipy>=1.10.0
pyyaml>=6.0
json5>=0.9.0

# 开发工具（可选）
black>=23.0.0
flake8>=6.0.0
isort>=5.12.0
pre-commit>=3.3.0